# Temaplte from: https://github.com/hyperledger-labs/documentation-template

name: 'Label stale issues and PRs'
permissions:
  contents: read

on:
  schedule:
    - cron: '30 1 * * *'

jobs:
  stale:
    runs-on: ubuntu-latest
    permissions:
      issues: write # apply the stale label and comment on issues
      pull-requests: write # apply the stale label and comment on PRs
    steps:
      - uses: actions/stale@v9
        with:
          stale-issue-message: 'This issue is stale because it has been open 30 days with no activity.'
          stale-pr-message: 'This PR is stale because it has been open 30 days with no activity.'
          stale-issue-label: 'stale'
          stale-pr-label: 'stale'
          days-before-stale: 30
          days-before-close: -1
          ignore-updates: true