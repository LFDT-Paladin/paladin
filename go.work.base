go 1.26.4

toolchain go1.26.4

// this just contains what's needed for the base-builder in docker
// By running this first, we get a big head start on the pre-reqs install
// for the build in a cached container
use (
	./config
	./common/go
	./sdk/go
	./toolkit/go
)
